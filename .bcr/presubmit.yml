## NOTE: rules_vmtest depends on rules_linux and rules_qemu which are not yet
## published to BCR. Before submitting to BCR, those modules must either be
## published first, or the local_path_override entries in MODULE.bazel must be
## replaced with appropriate registry references / patches.
matrix:
  platform:
    - ubuntu2204
    - macos_arm64
  bazel:
    - 8.x
    - 9.x
tasks:
  build_and_test:
    name: Build and test
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - //...
    test_targets:
      - //...
